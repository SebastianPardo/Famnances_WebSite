@model IEnumerable<Famnances.DataCore.Entities.ExpensesBudget>
@{
    ViewData["Title"] = "Index - Budget";
}

<div class="row d-flex justify-content-between">
    <div class="col col-md-8 ms-3 mb-3 mt-1">
        <a type="button" class="btn btn-lg rounded-pill btn-icon btn-primary card-header" asp-controller="Budgets" asp-action="Create" formmethod="get">
            <span class="tf-icons bx bx-plus"></span>
        </a>
    </div>
    <div class="col col-md-3 me-5 text-body-primary text-end">
        <h3 class="card-title"><strong>Budgets</strong></h3>
    </div>
</div>
<table class="table table-bordered">
    <thead>
        <tr>
            <th>Name</th>
            <th>Value</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <th>
                    @Html.DisplayFor(modelItem => item.Name)
                </th>
                <td>
                    @Html.DisplayFor(modelItem => item.Value)
                </td>
                <td>
                    <a asp-action="Details" class="btn btn-secondary" asp-route-id="@item.Id">Details</a>
                </td>
            </tr>
        }
    </tbody>
</table>